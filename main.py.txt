from flask import Flask, request

app = Flask(__name__)

@app.route('/')
def get_ip():
    return f"""
    <h1>Hello from Python Flask!</h1>
    <h2>Your IP Address: {request.remote_addr}</h2>
    """

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=80)